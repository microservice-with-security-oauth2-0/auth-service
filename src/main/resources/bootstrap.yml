spring:
  application:    name: auth-service
  cloud:
    config:
      fail-fast: true
      uri: http://config-server:8888
      username: user
      password: 1234
      retry:        max-attempts: 20
  